import { TestBed } from '@angular/core/testing';

import { Ofer } from './ofer.service';

/* describe('Ofer', () => {
  let service: Ofer;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Ofer);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */